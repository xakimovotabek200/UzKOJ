import React from "react";
import { Button } from "../../components";

const EditProblems = ({ item, getData }) => {
  return (
    <div>
      <Button className="fa-solid fa-edit text-xl text-white  rounded cursor-pointer bg-blue-500" />
    </div>
  );
};

export default EditProblems;
